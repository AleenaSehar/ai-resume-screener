GEMINI_API_KEY=your_gemini_api_key_here

# Comma-separated list of origins allowed to call this API (no trailing slashes).
# Include your deployed Netlify URL in production.
ALLOWED_ORIGINS=http://localhost:3000
